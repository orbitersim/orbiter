This folder contains source files and project files for an MFD that
serves as as sample how to access the gcAPI functionality.

It mainly serves as an example for how to initialize [gcInitialize()] and
get some information [gcEnabled(), gcClientID() and gcSketchpadVersion()]
regarding gcAPI.