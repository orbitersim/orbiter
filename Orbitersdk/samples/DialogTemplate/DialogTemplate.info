BEGIN_DESC
# Dialog Template

A simple SDK example which provides a dialog box that can be opened during a simulation session via the "My dialog"
entry in the Custom Functions list (Ctrl-F4).

This addon is not very useful - it is intended as a starting point for developers.
END_DESC
CATEGORY SDK sample plugins